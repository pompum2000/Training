@model IEnumerable<Training.Models.NhanVien>

@{
    ViewData["Title"] = "Index";
}

<h1>Danh sách nhân viên</h1>

@*
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdNv)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Position)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IdDepartment)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.IdNv)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Position)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IdDepartment)
            </td>
            <td>
                <a asp-controller="NhanViens" asp-action="Edit" asp-route-id="@item.IdNv">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdNv">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdNv">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>*@

<p>
    <a asp-controller="NhanViens" asp-action="Create" style="text-decoration:none" class="text-info"><i class="fas fa-plus-square"></i> Thêm nhân viên</a>
</p>
<table class="table">
    <thead>
        <tr>
        <th>IdNv</th>
        <th>Name</th>
        <th>Position</th>
        <th>IdDepartment</th>
        </tr>
    </thead>
    <tbody id="tableBody">
        
        
    </tbody>
</table>
 
